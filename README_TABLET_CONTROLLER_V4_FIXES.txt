Tablet controller v4 fixes

- ALL [AUTO] now starts a new isolated run every time and invalidates any previous run.
- Selecting another GFX, stage, or page stops ALL [AUTO] and removes the green active state.
- TAKE BUG now uses logo slot 1 or 2 from the first two PNG logos uploaded in the main controller logo library.
- The on-air logo slot is tracked so TAKE BUG and the active logo number show correctly.
- Event name display has an extra fallback for DJames pages that only show "Results on the Web" on the event index page.
