# Tablet/Main Controller V9 - Page Selector Limit

Change applied:

- Page Selector is capped to a maximum of 9 buttons on both the tablet controller and the main controller.
- ALL [AUTO] also runs only through the available pages up to a maximum of 9.
- Stage Selector remains unchanged with stages 1–20.
- No other tablet/controller behaviour was changed.
