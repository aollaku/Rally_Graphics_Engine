Safe area guides fix

- Safe area guides display only in the embedded controller preview monitor.
- /preview and /preview/live never display safe guides.
- /output and /output/live never display safe guides.
- The controller monitor adds ?controllerPreview=1 internally.
